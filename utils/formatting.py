def success(text: str) -> str:
  return f"✅ {text}"

def error(text: str) -> str:
  return f"❌ {text}"

def info(text: str) -> str:
  return f"ℹ️ {text}"

def header(title: str) -> str:
  return f"🏏 **{title}**\n"